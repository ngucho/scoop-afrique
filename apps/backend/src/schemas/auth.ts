/**
 * Auth schemas — Auth0 handles login/refresh.
 * This file is kept for backwards compatibility but no longer defines
 * login/refresh schemas. Auth0 SDK handles all that client-side.
 */
export {}
